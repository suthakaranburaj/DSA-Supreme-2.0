#include <iostream>
#include <vector>
#include <math.h>
#include<queue>

using namespace std;
void firstNonRepeatingElement(queue<char> &q){
    queue<char>store;
    while(!q.empty()){

    }
}
int main(){
    queue<char>q;
    q.push('a');
    q.push('b');
    q.push('c');
    q.push('d');
    q.push('e');
    q.push('f');

    firstNonRepeatingElement(q);
return 0;
}